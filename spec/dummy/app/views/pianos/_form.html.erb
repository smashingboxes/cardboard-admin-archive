<%= form_for(@piano) do |f| %>
  <% if @piano.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@piano.errors.count, "error") %> prohibited this piano from being saved:</h2>

      <ul>
      <% @piano.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
