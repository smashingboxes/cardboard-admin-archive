h1 Listing Pianos

table class="table table-striped"
  tr
    - for field in @pianos.column_names
      th= field
    th
    th

  - @pianos.each do |piano|
    tr
      - for field in piano.attributes.keys
        td = piano.attributes[field]

      td = link_to 'Edit', edit_admin_piano_path(piano)
      td = link_to 'Destroy', admin_piano_path(piano), :confirm => 'Are you sure?', :method => :delete

br

= link_to 'New Piano', new_admin_piano_path, class: "btn btn-primary"